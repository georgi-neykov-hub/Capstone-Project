<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

<!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
     dismiss it. -->
<!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->

    <PreferenceCategory
        android:title="General Settings" />

    <SwitchPreference
        android:key="notifications_enabled"
        android:title="@string/pref_title_notifications_for_episodes"
        android:summary="@string/pref_subtitle_notifications_for_episodes"
        android:defaultValue="true" />

    <Preference android:title="@string/pref_title_sync_settings">
        <intent android:action="@string/settingsAction"
            android:targetPackage="com.neykov.podcastportal"
            android:targetClass="com.neykov.podcastportal.view.settings.SettingsActivity"/>
    </Preference>

</PreferenceScreen>