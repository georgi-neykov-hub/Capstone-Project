<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/playlist_add_top"
        android:title="@string/label_add_playlist_top"
        android:icon="@drawable/ic_action_playlist_add"
        app:showAsAction="never"/>

    <item android:id="@+id/playlist_add_end"
        android:title="@string/label_add_playlist_end"
        android:icon="@drawable/ic_action_playlist_add"
        app:showAsAction="never"/>

    <item android:id="@+id/playlist_remove"
        android:title="@string/label_remove_playlist"
        android:icon="@drawable/ic_action_playlist_add"
        app:showAsAction="never"/>

    <item android:id="@+id/download"
        android:title="@string/label_download"
        android:icon="@drawable/ic_action_download"
        app:showAsAction="never"/>
</menu>